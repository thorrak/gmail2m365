# /etc/gmail2m365/m365.conf  (chmod 600)
# Entra ID app registration used for every mailbox. See README.md.
TENANT_ID="00000000-0000-0000-0000-000000000000"   # Directory (tenant) ID
CLIENT_ID="00000000-0000-0000-0000-000000000000"   # Application (client) ID
# The client secret itself goes in /etc/gmail2m365/m365.secret, one line, chmod 600.
